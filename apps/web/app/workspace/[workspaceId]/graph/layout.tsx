import type { ReactNode } from "react";

interface WorkspaceGraphLayoutProps {
  children: ReactNode;
}

export default function WorkspaceGraphLayout({
  children,
}: WorkspaceGraphLayoutProps) {
  return children;
}
